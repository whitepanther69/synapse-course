[
 {
  "lang": "Java",
  "ctx": "where the app keeps the api_key",
  "code": [
   "prefs.edit().putString(\"api_key\", value).apply()   // SharedPreferences, plaintext"
  ],
  "vuln": 0,
  "category": "Insecure storage",
  "options": [
   "Insecure storage",
   "Weak crypto",
   "Exported component",
   "Hardcoded secret"
  ],
  "why": "Plaintext SharedPreferences are readable on rooted devices or in backups.",
  "fixLines": [
   "store the api_key via ___ / EncryptedSharedPreferences"
  ],
  "slots": [
   "Keystore"
  ],
  "tokens": [
   "Keystore",
   "SD card",
   "logcat",
   "clipboard"
  ],
  "fixWhy": "Use the Android Keystore (EncryptedSharedPreferences); never store secrets in plaintext."
 },
 {
  "lang": "Java",
  "ctx": "the app stores the refresh_token",
  "code": [
   "prefs.edit().putString(\"refresh_token\", value).apply()   // SharedPreferences, plaintext"
  ],
  "vuln": 0,
  "category": "Insecure storage",
  "options": [
   "Insecure storage",
   "Weak crypto",
   "Exported component",
   "Hardcoded secret"
  ],
  "why": "Plaintext SharedPreferences are readable on rooted devices or in backups.",
  "fixLines": [
   "store the refresh_token via ___ / EncryptedSharedPreferences"
  ],
  "slots": [
   "Keystore"
  ],
  "tokens": [
   "Keystore",
   "SD card",
   "logcat",
   "clipboard"
  ],
  "fixWhy": "Use the Android Keystore (EncryptedSharedPreferences); never store secrets in plaintext."
 },
 {
  "lang": "Java",
  "ctx": "the app stores the token",
  "code": [
   "prefs.edit().putString(\"token\", value).apply()   // SharedPreferences, plaintext"
  ],
  "vuln": 0,
  "category": "Insecure storage",
  "options": [
   "Insecure storage",
   "Weak crypto",
   "Exported component",
   "Hardcoded secret"
  ],
  "why": "Plaintext SharedPreferences are readable on rooted devices or in backups.",
  "fixLines": [
   "store the token via ___ / EncryptedSharedPreferences"
  ],
  "slots": [
   "Keystore"
  ],
  "tokens": [
   "Keystore",
   "SD card",
   "logcat",
   "clipboard"
  ],
  "fixWhy": "Use the Android Keystore (EncryptedSharedPreferences); never store secrets in plaintext."
 },
 {
  "lang": "Java",
  "ctx": "where the app keeps the PIN",
  "code": [
   "prefs.edit().putString(\"PIN\", value).apply()   // SharedPreferences, plaintext"
  ],
  "vuln": 0,
  "category": "Insecure storage",
  "options": [
   "Insecure storage",
   "Weak crypto",
   "Exported component",
   "Hardcoded secret"
  ],
  "why": "Plaintext SharedPreferences are readable on rooted devices or in backups.",
  "fixLines": [
   "store the PIN via ___ / EncryptedSharedPreferences"
  ],
  "slots": [
   "Keystore"
  ],
  "tokens": [
   "Keystore",
   "SD card",
   "logcat",
   "clipboard"
  ],
  "fixWhy": "Use the Android Keystore (EncryptedSharedPreferences); never store secrets in plaintext."
 }
]