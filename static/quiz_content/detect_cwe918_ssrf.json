[
  {
    "id": "detect_cwe918_ssrf_0001",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String url = request.getParameter(\"url\");\nnew java.net.URL(url).openStream();",
    "options": [
      {
        "id": "A",
        "text": "Open Redirect",
        "correct": false
      },
      {
        "id": "B",
        "text": "XML External Entity (XXE)",
        "correct": false
      },
      {
        "id": "C",
        "text": "OS Command Injection",
        "correct": false
      },
      {
        "id": "D",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      }
    ],
    "explanation": "The value of `url` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0002",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String url = request.getParameter(\"url\");\nhttpClient.send(HttpRequest.newBuilder(java.net.URI.create(url)).build(), HttpResponse.BodyHandlers.ofString());",
    "options": [
      {
        "id": "A",
        "text": "Path Traversal",
        "correct": false
      },
      {
        "id": "B",
        "text": "OS Command Injection",
        "correct": false
      },
      {
        "id": "C",
        "text": "Cross-Site Request Forgery (CSRF)",
        "correct": false
      },
      {
        "id": "D",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      }
    ],
    "explanation": "The value of `url` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0003",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String url = request.getParameter(\"url\");\nrestTemplate.getForObject(url, String.class);",
    "options": [
      {
        "id": "A",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      },
      {
        "id": "B",
        "text": "XML External Entity (XXE)",
        "correct": false
      },
      {
        "id": "C",
        "text": "Path Traversal",
        "correct": false
      },
      {
        "id": "D",
        "text": "OS Command Injection",
        "correct": false
      }
    ],
    "explanation": "The value of `url` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0004",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String target = request.getParameter(\"target\");\nnew java.net.URL(target).openStream();",
    "options": [
      {
        "id": "A",
        "text": "Path Traversal",
        "correct": false
      },
      {
        "id": "B",
        "text": "OS Command Injection",
        "correct": false
      },
      {
        "id": "C",
        "text": "Cross-Site Request Forgery (CSRF)",
        "correct": false
      },
      {
        "id": "D",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      }
    ],
    "explanation": "The value of `target` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0005",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String target = request.getParameter(\"target\");\nhttpClient.send(HttpRequest.newBuilder(java.net.URI.create(target)).build(), HttpResponse.BodyHandlers.ofString());",
    "options": [
      {
        "id": "A",
        "text": "OS Command Injection",
        "correct": false
      },
      {
        "id": "B",
        "text": "XML External Entity (XXE)",
        "correct": false
      },
      {
        "id": "C",
        "text": "Cross-Site Request Forgery (CSRF)",
        "correct": false
      },
      {
        "id": "D",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      }
    ],
    "explanation": "The value of `target` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0006",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String target = request.getParameter(\"target\");\nrestTemplate.getForObject(target, String.class);",
    "options": [
      {
        "id": "A",
        "text": "OS Command Injection",
        "correct": false
      },
      {
        "id": "B",
        "text": "Path Traversal",
        "correct": false
      },
      {
        "id": "C",
        "text": "XML External Entity (XXE)",
        "correct": false
      },
      {
        "id": "D",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      }
    ],
    "explanation": "The value of `target` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0007",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String endpoint = request.getParameter(\"endpoint\");\nnew java.net.URL(endpoint).openStream();",
    "options": [
      {
        "id": "A",
        "text": "Cross-Site Request Forgery (CSRF)",
        "correct": false
      },
      {
        "id": "B",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      },
      {
        "id": "C",
        "text": "Open Redirect",
        "correct": false
      },
      {
        "id": "D",
        "text": "XML External Entity (XXE)",
        "correct": false
      }
    ],
    "explanation": "The value of `endpoint` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0008",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String endpoint = request.getParameter(\"endpoint\");\nhttpClient.send(HttpRequest.newBuilder(java.net.URI.create(endpoint)).build(), HttpResponse.BodyHandlers.ofString());",
    "options": [
      {
        "id": "A",
        "text": "Cross-Site Request Forgery (CSRF)",
        "correct": false
      },
      {
        "id": "B",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      },
      {
        "id": "C",
        "text": "Open Redirect",
        "correct": false
      },
      {
        "id": "D",
        "text": "Path Traversal",
        "correct": false
      }
    ],
    "explanation": "The value of `endpoint` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  },
  {
    "id": "detect_cwe918_ssrf_0009",
    "generated_from_template": "detect_cwe918_ssrf",
    "category": "detect",
    "skill_tag": "CWE-918",
    "owasp": "A10",
    "severity": "high",
    "difficulty": "intermediate",
    "linked_deck": null,
    "prompt": "Which vulnerability is most likely present in this server-side code?",
    "code_snippet": "String endpoint = request.getParameter(\"endpoint\");\nrestTemplate.getForObject(endpoint, String.class);",
    "options": [
      {
        "id": "A",
        "text": "Open Redirect",
        "correct": false
      },
      {
        "id": "B",
        "text": "Server-Side Request Forgery (SSRF)",
        "correct": true
      },
      {
        "id": "C",
        "text": "XML External Entity (XXE)",
        "correct": false
      },
      {
        "id": "D",
        "text": "OS Command Injection",
        "correct": false
      }
    ],
    "explanation": "The value of `endpoint` comes straight from request.getParameter and is fetched server-side. Because the server itself makes a request to an attacker-controlled address, this is Server-Side Request Forgery (CWE-918): the attacker can reach internal-only targets such as cloud metadata (169.254.169.254), localhost admin panels, or internal APIs the browser could never reach. It is NOT Open Redirect — the server fetches the URL itself rather than telling the user's browser to navigate. Fix: validate the URL against an allow-list of hosts and schemes, and block requests to internal/link-local IP ranges.",
    "source_reference": "CWE-918; OWASP A10:2021 (Server-Side Request Forgery)"
  }
]